// Component Styles
const styles = (theme) => ({
  root: {
    height: '100%',
    border: `1px solid ${theme.palette.common.black}`,
    borderRadius: '5px',
    width: '170px',
  },
});

export default styles;
